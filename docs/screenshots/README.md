# Скриншоты деплоя (Слой 3)

Сюда кладутся скриншоты демонстрации GitOps (см. [../DEPLOY.md](../DEPLOY.md)):

- `argocd-app-healthy.png` — UI ArgoCD: приложение `askops` в статусе Synced/Healthy.
- `kubectl-pods.png` — вывод `kubectl -n askops get pods` со всеми Running.
- `orchestrator-curl.png` — ответ оркестратора из кластера (необязательно).

Файлы добавляются вручную после прогона демо на локальном k3d-кластере.
