# Оркестратор: LangGraph + A2A-клиент + REST + роутер (LLM-классификация интента)
langgraph>=0.2.20
a2a-sdk==0.2.16
fastapi>=0.110
uvicorn[standard]>=0.29
pydantic>=2.6
httpx>=0.27
# Нужен роутеру для классификации интента через GigaChat (provider=gigachat).
# Лёгкий API-клиент без тяжёлых ML-зависимостей. Для openrouter хватает httpx.
gigachat>=0.1.30
prometheus-fastapi-instrumentator>=7
