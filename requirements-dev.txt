# Лёгкие зависимости для линта и тестов в CI.
# torch / sentence-transformers / gigachat НЕ нужны: тесты гоняют LLM в mock-режиме
# и мокают поиск, поэтому тяжёлые модели не качаются.
ruff>=0.5
pytest>=8.0

# Нужны, чтобы импортировались модули агентов:
opensearch-py>=2.4,<3
a2a-sdk==0.2.16
langgraph>=0.2.20
fastapi>=0.110
pydantic>=2.6
httpx>=0.27
requests>=2.31
# Аналитик (Слой 1): генератор синтетических тикетов.
faker>=25
